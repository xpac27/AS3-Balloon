package
{
    public class Leaf extends Entity
    {
        public function Leaf(subject:DisplayObjectContainer):void
        {
            _subject = subject;
        }
    }

    import flash.display.DisplayObjectContainer;

    import Entity;
}
